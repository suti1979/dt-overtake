## DT Overtake

- Server makefile gcp commands:

```
gcloud-docker-init
gcloud-docker-build
gcloud-docker-push
gcloud-run-deploy
```

- Local docker build & run:
  `docker-compose up`

- Local build & run:

```
/client
yarn
yarn dev

/server
yarn
yarn start:dev
```
